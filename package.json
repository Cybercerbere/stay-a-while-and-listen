{
  "name": "stay-a-while-and-listen",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --config vite.config.cs.ts --watch & vite build --config vite.config.bg.ts --watch",
    "build:cs": "vite build --config vite.config.cs.ts",
    "build:bg": "vite build --config vite.config.bg.ts",
    "build": "tsc -b && npm run build:cs && npm run build:bg",
    "preview": "vite preview",
    "zip": "npm run build &&zip -r stay-a-while-and-listen.zip manifest.json dist/ icons/"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "@types/node": "^25.5.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  }
}
